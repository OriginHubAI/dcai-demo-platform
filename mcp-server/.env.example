# MCP Server Configuration

# 后端 API 地址
BACKEND_API_URL=http://localhost:8000/api/v2

# 服务器配置 (SSE 模式)
MCP_HOST=0.0.0.0
MCP_PORT=8080

# 调试模式
DEBUG=false

# Django 设置模块路径 (用于导入模型)
DJANGO_SETTINGS_MODULE=core.settings
